import { EstadoTextoPipe } from './estado-texto.pipe';

describe('EstadoTextoPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoTextoPipe();
    expect(pipe).toBeTruthy();
  });
});
